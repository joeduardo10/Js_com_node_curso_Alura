const alunos = ['Joana', 'Juliana', 'Patricia','Ana'];
const media = [10, 8, 7.5, 9];

const lista = [alunos, media];
console.log(lista);
console.log(
    `a aluna da posição 1 da lista é: ${lista[0][1]} e a nota desa aluna é ${lista[1][1]}`
);